# 🔄 Инструкция по восстановлению эталонной версии

## Быстрое восстановление

### Вариант 1: Через Git тег
```bash
# Вернуться к эталонной версии
git checkout v1.0-stable

# Создать новую ветку от этой точки
git checkout -b feature/new-feature

# Или вернуть main к этой версии (ОСТОРОЖНО!)
git reset --hard v1.0-stable
```

### Вариант 2: Через коммит
```bash
# Вернуться к конкретному коммиту
git checkout f023a30

# Или сбросить текущую ветку
git reset --hard f023a30
```

## Проверка текущей версии

```bash
# Показать текущий коммит
git log --oneline -1

# Показать все теги
git tag -l

# Показать информацию о теге
git show v1.0-stable
```

## Что включено в эталонную версию v1.0-stable

### ✅ Функции
- Номер машины для водителей (vehicleNumber)
- Права доступа по компаниям для админов
- Локализация маркера склада
- Исправленные маршруты на карте
- ETA для водителей
- Цветные маршруты по водителям

### ✅ Файлы
- 65 файлов изменено
- 3005 строк добавлено
- 888 строк удалено

### ✅ Развертывание
- APK собран и протестирован
- Web задеплоен на Firebase
- Все функции работают

## Восстановление после экспериментов

Если вы внесли изменения и хотите вернуться:

```bash
# Отменить все незакоммиченные изменения
git reset --hard HEAD

# Вернуться к эталонной версии
git reset --hard v1.0-stable

# Пересобрать проект
flutter clean
flutter pub get
flutter build apk --release
flutter build web --release
```

## Создание резервной копии перед экспериментами

```bash
# Создать ветку от эталонной версии
git checkout v1.0-stable
git checkout -b backup/before-experiment

# Вернуться к main
git checkout main
```

## Сравнение с эталонной версией

```bash
# Показать отличия от эталонной версии
git diff v1.0-stable

# Показать список измененных файлов
git diff --name-only v1.0-stable

# Показать статистику изменений
git diff --stat v1.0-stable
```

## Частичное восстановление

Если нужно восстановить только определенные файлы:

```bash
# Восстановить один файл
git checkout v1.0-stable -- lib/models/user_model.dart

# Восстановить несколько файлов
git checkout v1.0-stable -- lib/models/ lib/services/

# Восстановить все файлы локализации
git checkout v1.0-stable -- lib/l10n/
```

## Проверка целостности

После восстановления проверьте:

```bash
# Проверить компиляцию
flutter analyze

# Запустить тесты (если есть)
flutter test

# Собрать APK
flutter build apk --release

# Проверить размер APK (должен быть ~33MB)
ls -lh build/app/outputs/flutter-apk/app-release.apk
```

## Контакты для помощи

Если возникли проблемы с восстановлением:
1. Проверьте CHANGELOG.md для деталей версии
2. Используйте `git log` для просмотра истории
3. Создайте issue с описанием проблемы

## Важные заметки

⚠️ **ВНИМАНИЕ**: 
- Команда `git reset --hard` удаляет все незакоммиченные изменения
- Всегда делайте резервную копию перед экспериментами
- Используйте ветки для новых функций

✅ **РЕКОМЕНДАЦИИ**:
- Создавайте новую ветку для каждой новой функции
- Регулярно коммитьте изменения
- Используйте осмысленные сообщения коммитов
- Тегируйте стабильные версии
