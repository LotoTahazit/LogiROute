# 📘 ACCOUNTING_CERTIFICATION.md

## LogiRoute — Стратегия сертификации бухгалтерского модуля

> Утверждено: 27.02.2026

---

## 1️⃣ Принцип: сертифицируется модуль, не платформа

Регулятору важно:
- Как формируется документ
- Как обеспечивается неизменяемость
- Как обеспечивается нумерация
- Как обеспечивается аудит
- Как хранятся данные
- Можно ли удалить/изменить задним числом

Регулятору НЕ важно:
- Маршруты, склад, диспетчер, GPS, UI

**→ Сертифицировать нужно Accounting Module, а не всю экосистему.**

---

## 2️⃣ Архитектура: Accounting как изолированный блок

Физически изолирован в Firestore:

```
companies/{companyId}/
  accounting/
    invoices/          — все типы документов
    receipts/          — квитанции
    credit_notes/      — зиккуй
    counters/          — счётчики нумерации
    audit_log/         — append-only лог событий
    integrity_chain/   — цепочка целостности
    backups/           — резервные копии
```

Warehouse и Logistics НЕ имеют права менять бухгалтерские документы.
Accounting НЕ зависит от Warehouse — может работать автономно.

---

## 3️⃣ Почему нельзя сертифицировать всё вместе

Если сертифицировать весь LogiRoute:
- Любое изменение в логистике/складе может потребовать повторной проверки
- Это замедлит развитие

Правильный подход:
> "Accounting module of LogiRoute is approved and compliant with regulations."

---

## 4️⃣ Уровень реализации: B (Priority / חשבשבת уровень)

### Уже реализовано:
- ✅ Immutable snapshot hash (SHA-256)
- ✅ Audit log (append-only, actorUid == auth.uid)
- ✅ Integrity chain (append-only)
- ✅ Sequential numbering (atomic transaction, +1, no gaps)
- ✅ Finalize flow (golden flow, порядок операций зафиксирован)
- ✅ Print events (append-only)
- ✅ Assignment number flow (מספר הקצאה)
- ✅ 5 типов документов (invoice, taxInvoiceReceipt, receipt, delivery, creditNote)
- ✅ Credit note вместо edit
- ✅ Статусы: active / cancelled / draft (delete запрещён)

### Нужно доработать:
- [ ] Firestore rules: полная блокировка delete для invoices
- [ ] Firestore rules: protected fields enforcement
- [ ] Firestore rules: module-based access (accounting entitlement)
- [ ] Экспорт документов (CSV/PDF batch)
- [ ] Реальный API רשות המסים (после регистрации)
- [ ] ח.פ клиента на PDF
- [ ] Логотип компании-клиента на PDF (из settings)
- [ ] Настраиваемая ставка НДС
- [ ] Полный технический документ для подачи

---

## 5️⃣ Чеклист для официального одобрения

### A. Невозможность удаления
- [ ] delete запрещён на уровне Firestore rules
- [ ] даже super_admin не может удалить завершённый документ
- [x] статус cancelled вместо delete

### B. Невозможность изменения
- [x] protected fields (ENGINEERING_METHODS.md §1.1)
- [x] immutableSnapshotHash
- [x] finalizedAt
- [x] audit log append-only

### C. Нумерация
- [x] атомарная (Firestore transaction)
- [x] без пропусков (+1)
- [x] клиент не может писать напрямую
- [x] отдельный счётчик для каждого типа документа

### D. Лог событий
- [x] кто (actorUid)
- [x] когда (serverTimestamp)
- [x] что сделал (action)
- [x] серверное время

### E. Экспорт
- [x] PDF генерация
- [ ] batch экспорт
- [ ] CSV экспорт

---

## 6️⃣ Стратегия запуска

1. ✅ Заморозить дизайн accounting (ENGINEERING_METHODS.md)
2. ✅ Написать технический документ (70% готово)
3. 🔄 Реализовать полностью (в процессе)
4. [ ] Провести внутренний аудит
5. [ ] Подать на одобрение

---

## 7️⃣ Влияние на модульность

Модульность остаётся без изменений:
- **Accounting** — строгий, сертифицируемый
- **Warehouse, Logistics, Dispatcher, Reports** — гибкие, развиваются свободно

---

## 8️⃣ Бизнес-модель

С официальным одобрением:
- Accounting модуль: 2,000–5,000 ₪/мес
- Warehouse/Logistics: отдельно
- 40–60 клиентов = $1M ARR

Преимущества:
- ✔ Повышает доверие
- ✔ Позволяет брать выше цену
- ✔ Снижает юридический риск
