# 🔍 Аудит безопасности и рефакторинга проекта LogiRoute

**Дата**: 14 февраля 2026  
**Версия**: v1.1-stable

---

## 📊 РЕЗЮМЕ

### Критические проблемы: 3
### Предупреждения: 15
### Рекомендации по оптимизации: 8

---

## 🚨 КРИТИЧЕСКИЕ УЯЗВИМОСТИ БЕЗОПАСНОСТИ

### 1. ❌ КРИТИЧНО: Пароли хранятся в открытом виде (SharedPreferences)

**Файл**: `lib/services/biometric_service.dart:88`

**Проблема**:
```dart
// ВАЖНО: В продакшене используйте flutter_secure_storage для хранения пароля!
await prefs.setString('biometric_password_$userId', password);
```

Пароли хранятся в `SharedPreferences` в открытом виде. На Android это обычный XML файл, доступный при root-доступе или через ADB backup.

**Решение**:
```dart
// Установить: flutter_secure_storage: ^9.0.0
import 'package:flutter_secure_storage/flutter_secure_storage.dart';

final storage = FlutterSecureStorage();
await storage.write(key: 'biometric_password_$userId', value: password);
```

**Риск**: Высокий - утечка паролей пользователей  
**Приоритет**: НЕМЕДЛЕННО

---

### 2. ⚠️ ВЫСОКИЙ: API ключи могут быть извлечены из APK

**Файл**: `.env`, `lib/services/api_config_service.dart`

**Проблема**:
Google Maps API ключи хранятся в `.env` файле и компилируются в APK. Их можно извлечь через декомпиляцию.

**Текущая защита**: Ограничения на уровне Google Cloud Console (по домену/пакету)

**Дополнительная защита**:
1. Используйте Firebase App Check для защиты API
2. Проксируйте запросы через свой backend
3. Используйте обфускацию кода (уже включена в release build)

**Риск**: Средний - возможна кража API квоты  
**Приоритет**: Высокий

---

### 3. ⚠️ СРЕДНИЙ: Отсутствует валидация SSL сертификатов

**Проблема**: В коде нет явной проверки SSL pinning для критичных запросов к Firebase и Google APIs.

**Решение**:
```dart
// Добавить SSL pinning для критичных эндпоинтов
import 'package:http_certificate_pinning/http_certificate_pinning.dart';
```

**Риск**: Средний - возможна MITM атака  
**Приоритет**: Средний

---

## 🗑️ НЕИСПОЛЬЗУЕМЫЕ ФАЙЛЫ И КОД

### Файлы для удаления:

1. **fix_interpolation.dart** - 229 ошибок компиляции, не используется
   - Путь: `fix_interpolation.dart`
   - Размер: ~1 KB
   - Действие: УДАЛИТЬ

2. **methods_to_insert.dart** - 100+ ошибок, временный файл
   - Путь: `methods_to_insert.dart`
   - Размер: ~5 KB
   - Действие: УДАЛИТЬ

3. **temp_input.txt** - временный файл
   - Путь: `temp_input.txt`
   - Действие: УДАЛИТЬ

4. **create_standalone_invoice_dialog.dart** - не завершен, 30+ ошибок
   - Путь: `lib/screens/dispatcher/create_standalone_invoice_dialog.dart`
   - Статус: Не используется, не завершен
   - Действие: УДАЛИТЬ или ЗАВЕРШИТЬ

5. **Дублирующиеся APK файлы**:
   - `web/app-release.apk` (33 MB)
   - `web/downloads/LogiRoute.apk` (33 MB)
   - `build/web/app-release.apk` (33 MB)
   - `build/web/downloads/LogiRoute.apk` (33 MB)
   - Действие: Оставить только в `build/app/outputs/flutter-apk/`

### Неиспользуемые методы:

1. **route_service.dart**:
   - `_getTruckHeight()` - не вызывается
   - `_calculateTruckWeight()` - не вызывается

2. **navigation_service.dart**:
   - `_createDetailedSteps()` - не вызывается

3. **navigation_widget.dart**:
   - `_decodePolyline()` - не вызывается
   - `_boundsFromLatLngList()` - не вызывается

4. **create_standalone_invoice_dialog.dart**:
   - Все методы не используются (файл не завершен)

### Неиспользуемые импорты:

1. `lib/models/invoice_status.dart:2` - `import 'invoice.dart'`
2. `lib/screens/dispatcher/create_standalone_invoice_dialog.dart` - 4 импорта

---

## 📦 БОЛЬШИЕ ФАЙЛЫ (требуют внимания)

### Build артефакты (можно удалить):

| Файл | Размер | Действие |
|------|--------|----------|
| `build/` папка целиком | ~500 MB | Добавить в .gitignore |
| `.dart_tool/flutter_build/` | ~100 MB | Уже в .gitignore |
| `android/.gradle/` | ~50 MB | Уже в .gitignore |

### Большие зависимости (нормально):

| Файл | Размер | Статус |
|------|--------|--------|
| `build/web/main.dart.js` | ~8 MB | ✅ Нормально (Flutter Web) |
| `build/web/canvaskit/*.wasm` | ~5 MB | ✅ Нормально (CanvasKit) |
| `functions/node_modules/` | ~50 MB | ✅ Нормально (Firebase Functions) |

---

## ⚠️ ПРЕДУПРЕЖДЕНИЯ АНАЛИЗАТОРА

### Критичные (требуют исправления):

1. **Deprecated API использование**:
   - `Color.withOpacity()` → использовать `.withValues()`
   - `dart:html` → использовать `package:web`
   - `dart:js` → использовать `dart:js_interop`
   - `desiredAccuracy` в Geolocator → использовать `settings`

2. **BuildContext across async gaps** (15 мест):
   - Может привести к крашам при unmount виджета
   - Нужно добавить проверки `if (!mounted) return;`

3. **Unused imports** (6 мест):
   - Увеличивают размер bundle
   - Действие: удалить

### Некритичные (можно игнорировать):

1. **Naming conventions**:
   - `VAT_RATE` константа (рекомендуется `vatRate`)
   - Параметр `sum` совпадает с типом

2. **Unnecessary cast** в `route_service.dart:184`

---

## 🔒 РЕКОМЕНДАЦИИ ПО БЕЗОПАСНОСТИ

### Немедленные действия:

1. ✅ **Заменить SharedPreferences на FlutterSecureStorage**
   ```bash
   flutter pub add flutter_secure_storage
   ```

2. ✅ **Добавить проверку SSL сертификатов**
   ```bash
   flutter pub add http_certificate_pinning
   ```

3. ✅ **Включить Firebase App Check**
   - Защита от злоупотребления API
   - Бесплатно до 500K запросов/месяц

4. ✅ **Добавить rate limiting на Firebase Functions**

### Долгосрочные улучшения:

1. **Аудит логов**:
   - Удалить `print()` с чувствительными данными
   - Использовать `debugPrint()` только для debug builds

2. **Обфускация**:
   - ✅ Уже включена в release build
   - Добавить ProGuard rules для дополнительной защиты

3. **Шифрование данных**:
   - Шифровать чувствительные данные в Firestore
   - Использовать Firebase Security Rules

---

## 🚀 РЕКОМЕНДАЦИИ ПО ОПТИМИЗАЦИИ

### 1. Уменьшение размера APK:

**Текущий размер**: 34 MB

**Оптимизации**:
```bash
# Разделить APK по архитектурам
flutter build apk --split-per-abi

# Результат: 3 APK по ~12 MB каждый
# - app-armeabi-v7a-release.apk (32-bit ARM)
# - app-arm64-v8a-release.apk (64-bit ARM)
# - app-x86_64-release.apk (64-bit x86)
```

**Экономия**: ~22 MB на устройство

### 2. Очистка build артефактов:

```bash
flutter clean
cd android && ./gradlew clean
```

### 3. Удаление неиспользуемых зависимостей:

Проверить `pubspec.yaml` на неиспользуемые пакеты:
```bash
flutter pub deps --no-dev | grep "^[a-z]"
```

### 4. Оптимизация изображений:

Проверить `assets/` на большие файлы:
- Использовать WebP вместо PNG
- Сжать изображения

### 5. Lazy loading:

Использовать deferred loading для редко используемых экранов:
```dart
import 'package:flutter/material.dart' deferred as material;
```

---

## 📋 ПЛАН ДЕЙСТВИЙ

### Приоритет 1 (Критично - сделать сейчас):

- [ ] Заменить SharedPreferences на FlutterSecureStorage для паролей
- [ ] Удалить файлы с ошибками (fix_interpolation.dart, methods_to_insert.dart)
- [ ] Удалить дублирующиеся APK из web/ и build/web/
- [ ] Добавить `build/` в .gitignore

### Приоритет 2 (Высокий - на этой неделе):

- [ ] Включить Firebase App Check
- [ ] Исправить BuildContext across async gaps
- [ ] Удалить неиспользуемые импорты
- [ ] Удалить неиспользуемые методы

### Приоритет 3 (Средний - в течение месяца):

- [ ] Обновить deprecated API
- [ ] Добавить SSL pinning
- [ ] Оптимизировать размер APK (split-per-abi)
- [ ] Аудит логов на чувствительные данные

### Приоритет 4 (Низкий - когда будет время):

- [ ] Исправить naming conventions
- [ ] Удалить unnecessary casts
- [ ] Оптимизировать изображения
- [ ] Добавить lazy loading

---

## 📊 МЕТРИКИ КОДА

### Статистика:

- **Всего файлов Dart**: ~50
- **Строк кода**: ~15,000
- **Ошибок компиляции**: 229 (в неиспользуемых файлах)
- **Предупреждений**: 15
- **Info сообщений**: 20

### Качество кода:

- ✅ Архитектура: Хорошая (разделение на services/screens/models)
- ✅ Читаемость: Хорошая (комментарии, локализация)
- ⚠️ Безопасность: Требует улучшений
- ⚠️ Тестирование: Отсутствует

---

## 🎯 ЗАКЛЮЧЕНИЕ

Проект в целом имеет хорошую архитектуру и функциональность, но требует:

1. **Немедленного исправления** критических уязвимостей безопасности
2. **Очистки** неиспользуемого кода и файлов
3. **Оптимизации** размера приложения

После выполнения рекомендаций приоритета 1-2, проект будет готов к продакшн использованию с высоким уровнем безопасности.

**Оценка времени на исправления**:
- Приоритет 1: 2-3 часа
- Приоритет 2: 4-6 часов
- Приоритет 3: 8-10 часов
- Приоритет 4: 4-6 часов

**Итого**: ~20 часов работы для полной оптимизации
