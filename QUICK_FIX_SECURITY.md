# ⚡ Быстрое исправление безопасности

## 🚨 ВЫПОЛНИТЕ ЭТИ ШАГИ ПРЯМО СЕЙЧАС:

### Шаг 1: Удалите .env из Git (2 минуты)
```bash
# Откройте терминал в корне проекта
cd D:\Projects_A\LogiRoute2

# Удалите .env из Git (файл останется локально)
git rm --cached .env

# Закоммитьте
git commit -m "Security: Remove .env file with API keys from repository"

# Отправьте в репозиторий
git push
```

### Шаг 2: Ротируйте Google Maps ключи (5 минут)

1. Откройте: https://console.cloud.google.com/apis/credentials
2. Найдите ключи:
   - `AIzaSyAw65vr-ynlQjOWWJv-bqN6x9S0onAQGW8` (Web)
   - `AIzaSyDs_vewHuQ2DK5r8yqvJ4W2jvUAusC3SkY` (Android)
3. Нажмите на каждый → "DELETE" → Подтвердите
4. Нажмите "CREATE CREDENTIALS" → "API key"
5. Создайте 2 новых ключа (Web и Android)

### Шаг 3: Настройте ограничения для новых ключей (3 минуты)

**Для Web ключа:**
1. Нажмите на новый ключ
2. Application restrictions → HTTP referrers
3. Добавьте:
   ```
   localhost:*
   ваш-домен.com/*
   ```
4. API restrictions → Restrict key
5. Выберите:
   - Maps JavaScript API
   - Geocoding API
   - Directions API
   - Places API
6. SAVE

**Для Android ключа:**
1. Нажмите на новый ключ
2. Application restrictions → Android apps
3. Добавьте package name и SHA-1 fingerprint
4. API restrictions → Restrict key
5. Выберите:
   - Maps SDK for Android
   - Geocoding API
   - Directions API
6. SAVE

### Шаг 4: Обновите .env файл (1 минута)
```bash
# Откройте .env в редакторе
notepad .env

# Замените старые ключи на новые:
GOOGLE_MAPS_WEB_KEY=ваш_новый_web_ключ
GOOGLE_MAPS_ANDROID_KEY=ваш_новый_android_ключ
```

### Шаг 5: Проверьте Firebase Security Rules (5 минут)

1. Откройте: https://console.firebase.google.com/
2. Выберите проект "LogiRoute"
3. Firestore Database → Rules
4. Убедитесь, что правила защищают данные:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Только аутентифицированные пользователи
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

5. Нажмите "Publish"

### Шаг 6: Настройте квоты (2 минуты)

1. Google Cloud Console → APIs & Services → Quotas
2. Найдите "Maps JavaScript API"
3. Установите дневной лимит (например, 10,000 запросов)
4. Настройте email алерты при 80% использования

## ✅ Проверка

После выполнения всех шагов:
- [ ] `.env` удален из Git репозитория
- [ ] Старые Google Maps ключи удалены
- [ ] Новые ключи созданы и настроены
- [ ] `.env` обновлен с новыми ключами
- [ ] Firebase Security Rules проверены
- [ ] Квоты настроены
- [ ] Приложение работает с новыми ключами

## 🔄 Тестирование

```bash
# Запустите приложение
flutter run -d chrome

# Проверьте:
# 1. Карта загружается
# 2. Геокодирование работает
# 3. Маршруты строятся
# 4. Нет ошибок в консоли
```

## 📞 Если что-то пошло не так

1. Проверьте, что новые ключи активированы (может занять 5 минут)
2. Проверьте, что API включены в Google Cloud Console
3. Проверьте консоль браузера на ошибки
4. Убедитесь, что `.env` файл загружается в приложении

## ⏱️ Общее время: ~20 минут

**ВАЖНО:** Не откладывайте! Чем дольше ключи открыты, тем выше риск злоупотребления.
