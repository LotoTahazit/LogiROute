# 📱 Руководство по сборке Android для LogiRoute

## 🔧 Исправленные проблемы

### ✅ Проблема #1: Неправильная конфигурация Firebase
**Решение:** Обновлены `appId` и `apiKey` в `firebase_options.dart` для соответствия `google-services.json`

### ✅ Проблема #2: Отсутствие ProGuard правил
**Решение:** Создан файл `android/app/proguard-rules.pro` с правилами для Firebase, Google Maps, и других плагинов

### ✅ Проблема #3: Неправильная инициализация MultiDex
**Решение:** Создан `LogiRouteApplication.kt` класс, наследующий `MultiDexApplication`

### ✅ Проблема #4: Несовместимость версий Firebase с SDK 36
**Решение:** 
- Обновлены версии `firebase_auth` и `cloud_firestore`
- Добавлен Firebase BOM 33.7.0 для управления версиями
- Явно указаны версии Google Play Services

### ✅ Проблема #5: Недостаточная обработка ошибок при инициализации
**Решение:** 
- Улучшена инициализация Firebase в `main.dart` с полным логированием
- Добавлена защита в `AuthService` от краша

---

## 🚀 Пошаговая инструкция по сборке

### 1. Очистка проекта

```bash
# Очистить Flutter
flutter clean

# Очистить Gradle кэш
cd android
./gradlew clean
cd ..

# Получить зависимости
flutter pub get
```

### 2. Сборка Debug версии

```bash
# APK
flutter build apk --debug

# Или установка напрямую на устройство
flutter run --debug
```

### 3. Сборка Release версии

```bash
# APK (для тестирования)
flutter build apk --release

# App Bundle (для Google Play)
flutter build appbundle --release
```

---

## 🐛 Отладка проблем

### Просмотр логов Android

```bash
# Все логи приложения
flutter logs

# Или через adb
adb logcat | grep -i logiroute

# Фильтр только ошибок
adb logcat *:E | grep -i firebase
```

### Проверка Firebase инициализации

В логах ищите:
- ✅ `Firebase initialized successfully` - все ок
- ❌ `Firebase initialization failed:` - смотрите детали ошибки

### Типичные ошибки и решения

#### ❌ `FirebaseApp name [DEFAULT] already exists`
**Причина:** Двойная инициализация Firebase  
**Решение:** Убедитесь, что `Firebase.initializeApp()` вызывается ТОЛЬКО ОДИН раз в `main()`

#### ❌ `MissingPluginException`
**Причина:** Плагин не зарегистрирован  
**Решение:** 
```bash
flutter clean
flutter pub get
flutter run
```

#### ❌ `PlatformException(error, com.google.firebase.FirebaseException: ...)`
**Причина:** Неправильная конфигурация Firebase  
**Решение:** Проверьте соответствие `firebase_options.dart` и `google-services.json`

#### ❌ Приложение вылетает сразу при запуске
**Причина:** Обычно ProGuard удалил нужные классы  
**Решение:** Проверьте `proguard-rules.pro` и пересоберите

---

## 📋 Чек-лист перед релизом

- [ ] Очищен кэш (`flutter clean`)
- [ ] Обновлены зависимости (`flutter pub get`)
- [ ] Проверены версии в `pubspec.yaml`
- [ ] Проверен `google-services.json`
- [ ] Проверен `firebase_options.dart`
- [ ] Протестирована Debug сборка на реальном устройстве
- [ ] Протестирована Release сборка
- [ ] Проверены ProGuard правила
- [ ] Проверены разрешения в `AndroidManifest.xml`
- [ ] Увеличен `versionCode` в `pubspec.yaml`

---

## 🔍 Проверка совместимости версий

### Firebase Plugins

| Плагин | Версия | Требует SDK |
|--------|--------|-------------|
| firebase_core | ^4.1.1 | 21+ |
| firebase_auth | ^6.2.3 | 21+ |
| cloud_firestore | ^6.0.3 | 21+ |

### Google Play Services

| Сервис | Версия |
|--------|--------|
| Firebase BOM | 33.7.0 |
| Maps | 18.2.0 |
| Location | 21.1.0 |

### Android SDK

| Параметр | Значение |
|----------|----------|
| compileSdk | 36 |
| targetSdk | 36 |
| minSdk | flutter.minSdkVersion |

---

## 💡 Советы по оптимизации

### 1. Уменьшение размера APK

- Используйте App Bundle вместо APK
- ProGuard уже включен в Release-режиме
- Ресурсы автоматически сжимаются

### 2. Ускорение сборки

```properties
# В android/gradle.properties уже настроено:
org.gradle.jvmargs=-Xmx4G
```

### 3. Отладка Release-сборки

Если нужно отлаживать Release:
```bash
flutter run --release --debug
```

---

## 📞 Контакты и поддержка

При возникновении проблем проверьте:
1. Логи Firebase Console
2. Логи Android Logcat
3. Версии плагинов в `pubspec.lock`

---

**Версия документа:** 1.0  
**Дата обновления:** 15.10.2025  
**Применимо к:** LogiRoute v1.0.0+1

