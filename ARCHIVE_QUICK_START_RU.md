# Быстрый старт: Автоматическая архивация

## Что это делает?

Система автоматически архивирует старые данные в Firebase Storage, чтобы не превысить бесплатные лимиты Firestore.

**Расписание:**
- 1-го числа каждого месяца в 02:00 → архивация истории инвентаря (старше 3 месяцев)
- 1-го числа каждого месяца в 03:00 → архивация завершенных заказов (старше 1 месяца)
- 15-го числа каждого месяца в 02:00 → удаление архивированных записей из Firestore (старше 6 месяцев)

**Важно:** Данные НЕ теряются! Они сохраняются в Firebase Storage и доступны через экран "Архивы".

## Установка (5 минут)

### 1. Установить Firebase CLI

```bash
npm install -g firebase-tools
```

### 2. Войти в Firebase

```bash
firebase login
```

### 3. Перейти в папку проекта

```bash
cd D:\Projects_A\LogiRoute2
```

### 4. Установить зависимости

```bash
cd functions
npm install
cd ..
```

### 5. Развернуть функции

```bash
firebase deploy --only functions
```

Готово! Функции будут запускаться автоматически по расписанию.

## Проверка

### В приложении

1. Войти как Админ или Диспетчер
2. Нажать иконку "архив" (📦) в AppBar
3. Посмотреть статистику и список архивов

### В Firebase Console

1. Открыть [Firebase Console](https://console.firebase.google.com/)
2. Выбрать проект LogiRoute
3. Перейти в **Functions** → увидите 3 функции:
   - `archiveInventoryHistory`
   - `archiveCompletedOrders`
   - `cleanupArchivedRecords`

### Просмотр логов

```bash
firebase functions:log
```

## Ручной запуск (для теста)

Если хотите протестировать прямо сейчас:

1. В приложении: Админ → Архивы → "ארכב היסטוריית מלאי"
2. Или в Firebase Console: Functions → выбрать функцию → "Test function"

## Стоимость

**Полностью бесплатно!**

- Cloud Functions: 2 млн вызовов/месяц бесплатно (мы используем 3 вызова/месяц)
- Firebase Storage: 5 GB бесплатно (мы используем ~20 MB/месяц)

## Что дальше?

Система работает автоматически. Рекомендуется:

1. Раз в месяц проверять экран "Архивы"
2. Следить за размером Storage в Firebase Console
3. При необходимости скачивать старые архивы на локальный компьютер

## Проблемы?

### "Insufficient permissions"

Нужно включить Blaze план (платный, но бесплатный лимит очень большой):
1. Firebase Console → Settings → Usage and billing
2. Upgrade to Blaze plan
3. Установить бюджетный алерт на $5

### Функции не запускаются

Проверьте логи:
```bash
firebase functions:log
```

### Нужна помощь?

Смотрите полную инструкцию в `ARCHIVE_SETUP.md`
