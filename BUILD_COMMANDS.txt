╔═══════════════════════════════════════════════════════════════╗
║       LOGIROUTE - КОМАНДЫ ДЛЯ СБОРКИ ANDROID                  ║
╚═══════════════════════════════════════════════════════════════╝

🧹 ОЧИСТКА ПРОЕКТА (при любых проблемах - НАЧАТЬ С ЭТОГО!)
═══════════════════════════════════════════════════════════════
flutter clean
cd android && ./gradlew clean && cd ..
flutter pub get

📱 УСТАНОВКА НА УСТРОЙСТВО
═══════════════════════════════════════════════════════════════
flutter run --debug          # Debug режим с hot reload
flutter run --release        # Release режим (для теста производительности)

🔨 СБОРКА APK
═══════════════════════════════════════════════════════════════
flutter build apk --debug    # Debug APK (для отладки)
flutter build apk --release  # Release APK (готовый к установке)
flutter build apk --split-per-abi --release  # Отдельные APK для каждой архитектуры (меньше размер)

📦 СБОРКА APP BUNDLE (для Google Play)
═══════════════════════════════════════════════════════════════
flutter build appbundle --release

🐛 ПРОСМОТР ЛОГОВ
═══════════════════════════════════════════════════════════════
flutter logs                                    # Все логи приложения
adb logcat | grep -i firebase                   # Только Firebase
adb logcat | grep -i "logiroute\|flutter"       # LogiRoute + Flutter
adb logcat *:E                                  # Только ошибки

🔍 ОТЛАДКА
═══════════════════════════════════════════════════════════════
flutter doctor -v            # Проверка окружения
flutter devices              # Список подключенных устройств
adb devices                  # Устройства через ADB

📊 АНАЛИЗ APK
═══════════════════════════════════════════════════════════════
flutter build apk --analyze-size  # Анализ размера APK

⚡ БЫСТРЫЙ ТЕСТ (при изменении кода)
═══════════════════════════════════════════════════════════════
1. flutter pub get
2. flutter run --debug
3. Изменить код
4. Нажать 'r' для hot reload или 'R' для hot restart

🚨 ЕСЛИ ПРИЛОЖЕНИЕ КРАШИТСЯ
═══════════════════════════════════════════════════════════════
1. flutter clean
2. cd android && ./gradlew clean && cd ..
3. flutter pub get
4. flutter run --debug
5. Смотреть логи: flutter logs

🎯 ПОЛНАЯ ПЕРЕСБОРКА (ЕСЛИ ВСЁ СЛОМАЛОСЬ)
═══════════════════════════════════════════════════════════════
flutter clean
cd android
./gradlew clean
./gradlew cleanBuildCache
cd ..
flutter pub get
flutter pub upgrade
flutter run --debug

🔑 ПОДПИСАНИЕ APK (для Release)
═══════════════════════════════════════════════════════════════
Уже настроено в android/app/build.gradle
Просто используйте: flutter build apk --release

📍 РАСПОЛОЖЕНИЕ ФАЙЛОВ
═══════════════════════════════════════════════════════════════
Debug APK:   build/app/outputs/flutter-apk/app-debug.apk
Release APK: build/app/outputs/flutter-apk/app-release.apk
App Bundle:  build/app/outputs/bundle/release/app-release.aab

═══════════════════════════════════════════════════════════════
💡 СОВЕТЫ:
- Всегда начинайте с flutter clean при проблемах
- Используйте --debug для разработки
- Тестируйте --release перед публикацией
- Проверяйте логи при крашах
═══════════════════════════════════════════════════════════════

