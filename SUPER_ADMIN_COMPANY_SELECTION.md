# План работ: Выбор компании для Super Admin

## Статус: В процессе

## Что уже сделано ✅

1. **Создан сервис выбора компании**
   - `lib/services/company_selection_service.dart`
   - Метод `getCompanyId(AuthService)` возвращает правильный companyId
   - Для super_admin - выбранная компания
   - Для обычных пользователей - их companyId

2. **Создан виджет выбора компании**
   - `lib/widgets/company_selector_widget.dart`
   - Показывается только для super_admin
   - Выпадающий список всех компаний

3. **Добавлен в админ-панель**
   - Виджет добавлен в AppBar админ-дашборда
   - Инициализация списка компаний в `initState()`

4. **Добавлен в Provider**
   - `CompanySelectionService` добавлен в `MultiProvider` в `main.dart`

## Что нужно сделать 🔧

### 1. Обновить все экраны для работы с выбранной компанией

Все экраны которые создают сервисы с `companyId` должны использовать `CompanySelectionService.getCompanyId()`:

#### Админ-панель
- [ ] `lib/screens/admin/admin_dashboard.dart`
  - Обновить `_loadUsers()` чтобы фильтровать по выбранной компании
  - Добавить `Consumer<CompanySelectionService>` для перезагрузки при смене компании

#### Диспетчер
- [ ] `lib/screens/dispatcher/dispatcher_dashboard.dart`
  - Обновить инициализацию `RouteService`
  - Добавить слушатель изменения компании

#### Склад
- [ ] `lib/screens/warehouse/warehouse_dashboard.dart`
  - Обновить инициализацию `InventoryService`
  - Обновить все диалоги создания/редактирования

- [ ] `lib/screens/warehouse/widgets/inventory_list_view.dart`
  - Обновить инициализацию `InventoryService`

- [ ] `lib/screens/warehouse/dialogs/add_inventory_dialog.dart`
  - Обновить инициализацию `InventoryService`

- [ ] `lib/screens/warehouse/dialogs/edit_inventory_dialog.dart`
  - Обновить инициализацию `InventoryService`

- [ ] `lib/screens/warehouse/inventory_count_screen.dart`
  - Обновить инициализацию `InventoryService`

#### Диалоги диспетчера
- [ ] `lib/screens/dispatcher/add_point_dialog.dart`
  - Обновить инициализацию `InventoryService` (3 места)
  - Обновить инициализацию `ClientService`
  - Обновить инициализацию `PriceService`

### 2. Паттерн обновления

Для каждого экрана нужно:

```dart
// Было:
late final RouteService _routeService;

@override
void initState() {
  super.initState();
  final companyId = context.read<AuthService>().userModel?.companyId ?? '';
  _routeService = RouteService(companyId: companyId);
}

// Стало:
late RouteService _routeService;

@override
void initState() {
  super.initState();
  _initializeServices();
}

void _initializeServices() {
  final authService = context.read<AuthService>();
  final companyService = context.read<CompanySelectionService>();
  final companyId = companyService.getCompanyId(authService) ?? '';
  
  _routeService = RouteService(companyId: companyId);
  // Загрузить данные
}

@override
Widget build(BuildContext context) {
  return Consumer<CompanySelectionService>(
    builder: (context, companyService, child) {
      // Перезагрузить данные при смене компании
      return YourWidget();
    },
  );
}
```

### 3. Обновить сервисы которые еще не мигрированы

Эти сервисы работают с ROOT коллекциями и нужно их обновить для работы с nested collections:

- [ ] **CompanySettingsService**
  - Изменить путь: `companySettings` → `companies/{companyId}/company_info`
  - Обновить все методы

- [ ] **NotificationService** (если есть)
  - Изменить путь: `notifications` → `companies/{companyId}/notifications`

- [ ] **DriverLocationService** (если есть)
  - Изменить путь: `driver_locations` → `companies/{companyId}/driver_locations`

- [ ] **BackupService** (если есть)
  - Изменить путь: `backups` → `companies/{companyId}/backups`

- [ ] **InventoryCountService** (если есть)
  - Изменить путь: `inventory_counts` → `companies/{companyId}/inventory_counts`

- [ ] **DailySummaryService** (если есть)
  - Изменить путь: `daily_summaries` → `companies/{companyId}/daily_summaries`

### 4. Добавить локализацию

Добавить переводы для виджета выбора компании:

```dart
// lib/l10n/app_localizations.dart
String get selectCompany;

// lib/l10n/app_localizations_he.dart
@override
String get selectCompany => 'בחר חברה';

// lib/l10n/app_localizations_ru.dart
@override
String get selectCompany => 'Выбрать компанию';

// lib/l10n/app_localizations_en.dart
@override
String get selectCompany => 'Select Company';
```

### 5. Тестирование

После обновления всех экранов:

1. Войти как super_admin
2. Выбрать компанию из списка
3. Проверить что данные обновляются:
   - Список пользователей в админ-панели
   - Точки доставки в диспетчере
   - Инвентарь на складе
   - Клиенты
   - Цены
   - Типы коробок
4. Создать новые данные - они должны создаваться в выбранной компании
5. Переключить компанию - данные должны обновиться

### 6. Оптимизация

После базовой реализации:

- [ ] Добавить кеширование данных
- [ ] Добавить индикатор загрузки при смене компании
- [ ] Добавить сохранение выбранной компании в localStorage
- [ ] Добавить проверку прав доступа к компании

## Приоритет задач

1. **Высокий**: Обновить админ-панель (пользователи)
2. **Высокий**: Обновить диспетчер (точки доставки, клиенты)
3. **Средний**: Обновить склад (инвентарь)
4. **Средний**: Обновить сервисы (CompanySettingsService и др.)
5. **Низкий**: Локализация
6. **Низкий**: Оптимизация

## Примечания

- Все изменения должны быть обратно совместимы
- Обычные пользователи не должны заметить изменений
- Super_admin должен видеть данные только выбранной компании
- При смене компании все данные должны перезагружаться

## Текущий статус

✅ Инфраструктура готова (сервис + виджет)
🔧 Нужно обновить экраны для использования выбранной компании
⏳ Локализация не добавлена
⏳ Тестирование не проведено
