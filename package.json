{
  "name": "firestore-rules-tests",
  "private": true,
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.0",
    "firebase": "^11.0.0",
    "firebase-tools": "^13.0.0"
  },
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore \"node --test\""
  }
}
